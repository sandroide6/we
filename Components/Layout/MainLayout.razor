@inherits LayoutComponentBase
@implements IDisposable
@using Microsoft.JSInterop
@inject CarritoService CarritoService
@inject UsuarioService UsuarioService
@inject NavigationManager NavigationManager
@inject IJSRuntime JSRuntime

<div class="page">
    <header class="top-bar">
        <div class="container">
            <a href="/" class="logo">
                <span class="logo-icon">⚡</span>
                <span class="logo-text">TechStore</span>
            </a>
            
            <nav class="nav-menu">
                <a href="/" class="nav-link @(IsActive("/") ? "active" : "")">Catálogo</a>
                @if (UsuarioService.UsuarioActual != null)
                {
                    <a href="/mis-pedidos" class="nav-link @(IsActive("/mis-pedidos") ? "active" : "")">Mis Pedidos</a>
                    <a href="/favoritos" class="nav-link @(IsActive("/favoritos") ? "active" : "")">❤️ Favoritos</a>
                }
            </nav>

            <div class="top-bar-right">
                @if (UsuarioService.UsuarioActual != null)
                {
                    <div class="user-menu">
                        <img src="@UsuarioService.UsuarioActual.AvatarUrl" alt="@UsuarioService.UsuarioActual.Nombre" class="user-avatar" />
                        <span class="user-greeting">@UsuarioService.UsuarioActual.Nombre</span>
                        <div class="user-dropdown">
                            <a href="/perfil" class="dropdown-item">👤 Mi Perfil</a>
                            <a href="/mis-pedidos" class="dropdown-item">📦 Mis Pedidos</a>
                            <a href="/favoritos" class="dropdown-item">❤️ Favoritos</a>
                            <hr />
                            <button @onclick="Logout" class="dropdown-item logout-btn">🚪 Cerrar Sesión</button>
                        </div>
                    </div>
                }
                else
                {
                    <div class="auth-buttons">
                        <a href="/login" class="btn-login-header">Iniciar Sesión</a>
                        <a href="/register" class="btn-register-header">Registrarse</a>
                    </div>
                }
            </div>
        </div>
    </header>

    <main class="main-content">
        <div class="container">
            @Body
        </div>
    </main>

    <CarritoFloating />

    <footer class="footer">
        <div class="container">
            <div class="footer-content">
                <div class="footer-section">
                    <h3>TechStore</h3>
                    <p>Tu tienda de tecnología de confianza</p>
                </div>
                <div class="footer-section">
                    <h4>Enlaces</h4>
                    <a href="/">Catálogo</a>
                    <a href="/about">Sobre Nosotros</a>
                    <a href="/contact">Contacto</a>
                </div>
                <div class="footer-section">
                    <h4>Soporte</h4>
                    <a href="/help">Ayuda</a>
                    <a href="/shipping">Envíos</a>
                    <a href="/returns">Devoluciones</a>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; 2024 TechStore - Todos los derechos reservados</p>
            </div>
        </div>
    </footer>
</div>

@code {
    protected override void OnInitialized()
    {
        CarritoService.OnChange += StateHasChanged;
        UsuarioService.OnChange += StateHasChanged;
    }

    public void Dispose()
    {
        CarritoService.OnChange -= StateHasChanged;
        UsuarioService.OnChange -= StateHasChanged;
    }

    private bool IsActive(string path)
    {
        return NavigationManager.Uri.EndsWith(path);
    }

    private async Task Logout()
    {
        try
        {
            await JSRuntime.InvokeVoidAsync("authFunctions.logout");
            NavigationManager.NavigateTo("/login", forceLoad: true);
        }
        catch
        {
            NavigationManager.NavigateTo("/login", forceLoad: true);
        }
    }
}
